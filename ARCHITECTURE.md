# EduScan System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENT LAYER                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  Browser │  │  Mobile  │  │  Tablet  │  │   QR     │       │
│  │          │  │          │  │          │  │  Scanner │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER (Next.js)                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Landing Page  │  Auth Pages  │  Dashboards  │  Forms    │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  React Components  │  Layouts  │  Hooks  │  Utilities    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                    APPLICATION LAYER (API Routes)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │   Auth   │  │  Users   │  │ Sessions │  │Attendance│       │
│  │   API    │  │   API    │  │   API    │  │   API    │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │   Auth   │  │  User    │  │ Session  │  │Attendance│       │
│  │ Services │  │ Services │  │ Services │  │ Services │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                    DATA ACCESS LAYER (Prisma ORM)                │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Prisma Client  │  Models  │  Migrations  │  Queries     │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                    DATABASE LAYER (PostgreSQL)                   │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │  Tables  │  │  Indexes │  │Relations │  │  Views   │       │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

### User Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │ 1. Enter credentials
     ↓
┌─────────────────┐
│  Login Page     │
└────┬────────────┘
     │ 2. POST /api/auth/login
     ↓
┌─────────────────┐
│  Login API      │
└────┬────────────┘
     │ 3. Verify credentials
     ↓
┌─────────────────┐
│  Database       │ ← Check user exists, password matches
└────┬────────────┘
     │ 4. User data
     ↓
┌─────────────────┐
│  Login API      │ ← Generate JWT token
└────┬────────────┘
     │ 5. Return token + user data
     ↓
┌─────────────────┐
│  Login Page     │ ← Store token, redirect to dashboard
└────┬────────────┘
     │ 6. Navigate to dashboard
     ↓
┌─────────────────┐
│  Dashboard      │ ← Display user-specific content
└─────────────────┘
```

### Attendance Marking Flow (QR Code)

```
┌─────────┐
│ Teacher │
└────┬────┘
     │ 1. Create Session
     ↓
┌──────────────────┐
│ Session Creation │
│      Form        │
└────┬─────────────┘
     │ 2. POST /api/sessions
     ↓
┌──────────────────┐
│  Sessions API    │ ← Generate QR code
└────┬─────────────┘
     │ 3. Store session
     ↓
┌──────────────────┐
│   Database       │
└────┬─────────────┘
     │ 4. Session created
     ↓
┌──────────────────┐
│  QR Code Display │ ← Show QR code to students
└────┬─────────────┘
     │
     │        ┌─────────┐
     │        │ Student │
     │        └────┬────┘
     │             │ 5. Scan QR code
     │             ↓
     │        ┌─────────────────┐
     │        │ Camera/Scanner  │
     │        └────┬────────────┘
     │             │ 6. POST /api/attendance/mark
     │             ↓
     │        ┌─────────────────┐
     │        │ Attendance API  │ ← Verify QR, session, user
     │        └────┬────────────┘
     │             │ 7. Create attendance record
     │             ↓
     │        ┌─────────────────┐
     │        │   Database      │
     │        └────┬────────────┘
     │             │ 8. Attendance marked
     │             ↓
     │        ┌─────────────────┐
     │        │ Confirmation    │
     │        └─────────────────┘
     │
     ↓
┌──────────────────┐
│  Real-time       │ ← Update attendance count
│  Dashboard       │
└──────────────────┘
```

## 🗂️ Database Schema Relationships

```
┌──────────────┐
│Organization  │
│              │
│ - id         │
│ - name       │
│ - type       │
│ - email      │
└──────┬───────┘
       │
       │ has many
       ↓
┌──────────────────────┐
│       User           │
│                      │
│ - id                 │
│ - email              │
│ - role               │
│ - organizationId  ←──┘
└───┬─────────┬────────┘
    │         │
    │ creates │ attends
    │         │
    ↓         ↓
┌────────┐  ┌──────────────┐
│Session │  │AttendanceRec │
│        │  │              │
│- id    │  │- sessionId   │
│- name  │  │- userId      │
└───┬────┘  │- status      │
    │       │- method      │
    │ has   └──────────────┘
    │ many
    │
    ↓
┌────────────┐
│  Course    │
│            │
│ - id       │
│ - name     │
└──┬─────────┘
   │
   │ belongs to
   ↓
┌────────────┐
│Department  │
│            │
│ - id       │
│ - name     │
└────────────┘
```

## 🎯 Component Architecture

### Dashboard Component Hierarchy

```
DashboardLayout
│
├── Sidebar
│   ├── Logo
│   ├── Navigation Items
│   │   ├── Dashboard Link
│   │   ├── Users Link
│   │   ├── Sessions Link
│   │   └── Settings Link
│   └── User Profile
│       ├── Avatar
│       └── Logout Button
│
├── TopBar
│   ├── Organization Name
│   ├── Notification Bell
│   └── Mobile Menu Button
│
└── Main Content
    ├── Admin Dashboard
    │   ├── Welcome Banner
    │   ├── Stats Grid
    │   │   ├── Total Users Card
    │   │   ├── Active Sessions Card
    │   │   ├── Attendance Today Card
    │   │   └── Average Rate Card
    │   ├── Charts Section
    │   │   ├── Weekly Chart
    │   │   └── Methods Chart
    │   └── Recent Sessions List
    │
    ├── Teacher Dashboard
    │   ├── My Sessions
    │   ├── Students List
    │   └── Analytics
    │
    └── Class Rep Dashboard
        ├── Active Sessions
        └── Class Statistics
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────┐
│         SECURITY LAYERS                      │
│                                              │
│  ┌────────────────────────────────────┐    │
│  │  1. HTTPS/TLS Encryption           │    │
│  │     - Encrypted data transmission  │    │
│  └────────────────────────────────────┘    │
│                    ↓                         │
│  ┌────────────────────────────────────┐    │
│  │  2. Authentication Layer           │    │
│  │     - JWT tokens                   │    │
│  │     - Password hashing (bcrypt)    │    │
│  │     - Session management           │    │
│  └────────────────────────────────────┘    │
│                    ↓                         │
│  ┌────────────────────────────────────┐    │
│  │  3. Authorization Layer            │    │
│  │     - Role-based access control    │    │
│  │     - Permission checking          │    │
│  │     - Admin-only operations        │    │
│  └────────────────────────────────────┘    │
│                    ↓                         │
│  ┌────────────────────────────────────┐    │
│  │  4. Data Validation Layer          │    │
│  │     - Input sanitization           │    │
│  │     - Type checking (TypeScript)   │    │
│  │     - Schema validation (Zod)      │    │
│  └────────────────────────────────────┘    │
│                    ↓                         │
│  ┌────────────────────────────────────┐    │
│  │  5. Database Security Layer        │    │
│  │     - SQL injection prevention     │    │
│  │     - Parameterized queries        │    │
│  │     - Data encryption at rest      │    │
│  └────────────────────────────────────┘    │
│                    ↓                         │
│  ┌────────────────────────────────────┐    │
│  │  6. Audit & Monitoring             │    │
│  │     - Action logging               │    │
│  │     - Error tracking               │    │
│  │     - Access logs                  │    │
│  └────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

## 📱 Responsive Design Breakpoints

```
Mobile First Approach:

320px - 640px     641px - 768px      769px - 1024px     1025px+
┌─────────────┐   ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
│   Mobile    │   │   Mobile L   │   │    Tablet    │   │   Desktop    │
│             │   │              │   │              │   │              │
│ - Stack     │   │ - Stack      │   │ - 2 columns  │   │ - Full grid  │
│ - Hamburger │   │ - Bigger     │   │ - Sidebar    │   │ - Sidebar    │
│ - Single    │   │   text       │   │   toggle     │   │   always on  │
│   column    │   │ - Touch      │   │ - Touch OK   │   │ - Hover      │
│             │   │   friendly   │   │              │   │   effects    │
└─────────────┘   └──────────────┘   └──────────────┘   └──────────────┘
```

## 🔄 State Management

```
┌────────────────────────────────────────────┐
│          APPLICATION STATE                  │
│                                             │
│  ┌────────────────────────────────────┐   │
│  │  Server State (API Data)           │   │
│  │  - User data                       │   │
│  │  - Sessions                        │   │
│  │  - Attendance records              │   │
│  │  - Analytics                       │   │
│  └────────────────────────────────────┘   │
│                    ↕                        │
│  ┌────────────────────────────────────┐   │
│  │  Client State (React State)        │   │
│  │  - Form inputs                     │   │
│  │  - UI state                        │   │
│  │  - Loading states                  │   │
│  │  - Error messages                  │   │
│  └────────────────────────────────────┘   │
│                    ↕                        │
│  ┌────────────────────────────────────┐   │
│  │  Persistent State (localStorage)   │   │
│  │  - JWT token                       │   │
│  │  - User preferences                │   │
│  │  - Theme settings                  │   │
│  └────────────────────────────────────┘   │
└────────────────────────────────────────────┘
```

## 🚀 Deployment Architecture

```
┌─────────────────────────────────────────────────────────┐
│                     PRODUCTION                           │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │           Load Balancer / CDN                  │    │
│  │            (Vercel Edge Network)               │    │
│  └─────────────┬──────────────────────────────────┘    │
│                │                                         │
│                ↓                                         │
│  ┌────────────────────────────────────────────────┐    │
│  │         Next.js Application Instances          │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐    │    │
│  │  │Instance 1│  │Instance 2│  │Instance N│    │    │
│  │  └──────────┘  └──────────┘  └──────────┘    │    │
│  └─────────────┬──────────────────────────────────┘    │
│                │                                         │
│                ↓                                         │
│  ┌────────────────────────────────────────────────┐    │
│  │         PostgreSQL Database Cluster            │    │
│  │  ┌──────────────┐       ┌──────────────┐     │    │
│  │  │   Primary    │  ───► │   Replica    │     │    │
│  │  │   Database   │       │   Database   │     │    │
│  │  └──────────────┘       └──────────────┘     │    │
│  └────────────────────────────────────────────────┘    │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │         External Services (Optional)           │    │
│  │  - Email Service (SendGrid/SES)               │    │
│  │  - SMS Service (Twilio)                       │    │
│  │  - File Storage (S3)                          │    │
│  │  - Monitoring (Sentry/DataDog)               │    │
│  └────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────┘
```

## 🧩 Module Dependencies

```
Landing Page
    ├── Framer Motion (animations)
    ├── Lucide React (icons)
    └── Next.js Link (navigation)

Authentication
    ├── JWT (token management)
    ├── bcryptjs (password hashing)
    └── Zod (validation)

Dashboards
    ├── Recharts (data visualization)
    ├── date-fns (date formatting)
    └── DashboardLayout (shared component)

Database
    ├── Prisma Client (ORM)
    ├── PostgreSQL (database)
    └── Connection pooling

API Routes
    ├── Next.js handlers
    ├── Prisma (database access)
    └── Auth utilities (JWT)
```

## 📊 Performance Optimization

```
┌─────────────────────────────────────────────┐
│      PERFORMANCE STRATEGIES                  │
│                                              │
│  Frontend:                                   │
│  ├── Code splitting (automatic)             │
│  ├── Image optimization                     │
│  ├── Lazy loading components                │
│  ├── CSS minification                       │
│  └── Tree shaking                           │
│                                              │
│  Backend:                                    │
│  ├── Database indexing                      │
│  ├── Connection pooling                     │
│  ├── Query optimization                     │
│  ├── Caching strategies                     │
│  └── API response compression               │
│                                              │
│  Deployment:                                 │
│  ├── CDN for static assets                  │
│  ├── Edge functions                         │
│  ├── Serverless architecture                │
│  ├── Auto-scaling                           │
│  └── Load balancing                         │
└─────────────────────────────────────────────┘
```

---

This architecture is designed for scalability, security, and maintainability. Each layer is independent and can be scaled or modified without affecting other layers.
